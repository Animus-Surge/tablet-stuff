--Testing script for scripting engine

--Note: all functions are optional, you can remove them if you don't need them;

ready = function()
    --Ready function, called when the script is loaded;
end

update = function()
    --Update function, called every frame;
end

fixed_update = function(delta)
    --Fixed update, called every frame with delta time as an argument;
end

draw = function()
    --Draw function, called every frame;
end

input = function(event)
    --Input function, called when an input event is detected;
end

shutdown = function()
    --Shutdown function, called when the script is unloaded;
end
